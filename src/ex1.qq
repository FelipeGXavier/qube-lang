val x = true;
val t = 'teste';
val y = 10.5;
val z = 10*2*3+10-2;
if (x) {
    return x;
}else {
    return false;
}
val add = def(a,b) {
    return a + b;
}
val result = add(z, 2);
if (result <> 10) {
    return false;
}